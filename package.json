{
  "name": "cryptopulse-dashboard",
  "version": "3.1.0",
  "private": true,
  "type": "module",
  "description": "CryptoPulse v3.1 - polished professional crypto market dashboard with improved UX/UI, no-login local workspace, TypeScript, analytics, tests, CI and Vercel-ready release.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit --pretty",
    "test": "vitest --watch",
    "test:run": "vitest run",
    "quality": "npm run typecheck && npm run test:run && npm run build"
  },
  "dependencies": {
    "lucide-react": "^0.468.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "recharts": "^2.13.3"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.11",
    "typescript": "^5.6.3",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "vitest": "^2.1.9"
  }
}
